rule Number_ : js {
    strings:
        $ = "Number"
    condition:
        all of them
}
rule String_ : js {
    strings:
        $ = "String"
    condition:
        all of them
}
rule UTC_ : js {
    strings:
        $ = "UTC"
    condition:
        all of them
}
rule anchor_ : js {
    strings:
        $ = "anchor"
    condition:
        all of them
}
rule big_ : js {
    strings:
        $ = "big"
    condition:
        all of them
}
rule blink_ : js {
    strings:
        $ = "blink"
    condition:
        all of them
}
rule bold_ : js {
    strings:
        $ = "bold"
    condition:
        all of them
}
rule charAt_ : js {
    strings:
        $ = "charAt"
    condition:
        all of them
}
rule charCodeAt_ : js {
    strings:
        $ = "charCodeAt"
    condition:
        all of them
}
rule compile_ : js {
    strings:
        $ = "compile"
    condition:
        all of them
}
rule concat_ : js {
    strings:
        $ = "concat"
    condition:
        all of them
}
rule copyWithin_ : js {
    strings:
        $ = "copyWithin"
    condition:
        all of them
}
rule decodeURI_ : js {
    strings:
        $ = "decodeURI"
    condition:
        all of them
}
rule decodeURIComponent_ : js {
    strings:
        $ = "decodeURIComponent"
    condition:
        all of them
}
rule encodeURI_ : js {
    strings:
        $ = "encodeURI"
    condition:
        all of them
}
rule encodeURIComponent_ : js {
    strings:
        $ = "encodeURIComponent"
    condition:
        all of them
}
rule endsWith_ : js {
    strings:
        $ = "endsWith"
    condition:
        all of them
}
rule escape_ : js {
    strings:
        $ = "escape"
    condition:
        all of them
}
rule eval_ : js {
    strings:
        $ = "eval"
    condition:
        all of them
}
rule every_ : js {
    strings:
        $ = "every"
    condition:
        all of them
}
rule exec_ : js {
    strings:
        $ = "exec"
    condition:
        all of them
}
rule fill_ : js {
    strings:
        $ = "fill"
    condition:
        all of them
}
rule filter_ : js {
    strings:
        $ = "filter"
    condition:
        all of them
}
rule find_ : js {
    strings:
        $ = "find"
    condition:
        all of them
}
rule findIndex_ : js {
    strings:
        $ = "findIndex"
    condition:
        all of them
}
rule fixed_ : js {
    strings:
        $ = "fixed"
    condition:
        all of them
}
rule fontcolor_ : js {
    strings:
        $ = "fontcolor"
    condition:
        all of them
}
rule fontsize_ : js {
    strings:
        $ = "fontsize"
    condition:
        all of them
}
rule forEach_ : js {
    strings:
        $ = "forEach"
    condition:
        all of them
}
rule fromCharCode_ : js {
    strings:
        $ = "fromCharCode"
    condition:
        all of them
}
rule getDate_ : js {
    strings:
        $ = "getDate"
    condition:
        all of them
}
rule getDay_ : js {
    strings:
        $ = "getDay"
    condition:
        all of them
}
rule getFullYear_ : js {
    strings:
        $ = "getFullYear"
    condition:
        all of them
}
rule getHours_ : js {
    strings:
        $ = "getHours"
    condition:
        all of them
}
rule getMilliseconds_ : js {
    strings:
        $ = "getMilliseconds"
    condition:
        all of them
}
rule getMinutes_ : js {
    strings:
        $ = "getMinutes"
    condition:
        all of them
}
rule getMonth_ : js {
    strings:
        $ = "getMonth"
    condition:
        all of them
}
rule getSeconds_ : js {
    strings:
        $ = "getSeconds"
    condition:
        all of them
}
rule getTime_ : js {
    strings:
        $ = "getTime"
    condition:
        all of them
}
rule getTimezoneOffset_ : js {
    strings:
        $ = "getTimezoneOffset"
    condition:
        all of them
}
rule getUTCDate_ : js {
    strings:
        $ = "getUTCDate"
    condition:
        all of them
}
rule getUTCDay_ : js {
    strings:
        $ = "getUTCDay"
    condition:
        all of them
}
rule getUTCFullYear_ : js {
    strings:
        $ = "getUTCFullYear"
    condition:
        all of them
}
rule getUTCHours_ : js {
    strings:
        $ = "getUTCHours"
    condition:
        all of them
}
rule getUTCMilliseconds_ : js {
    strings:
        $ = "getUTCMilliseconds"
    condition:
        all of them
}
rule getUTCMinutes_ : js {
    strings:
        $ = "getUTCMinutes"
    condition:
        all of them
}
rule getUTCMonth_ : js {
    strings:
        $ = "getUTCMonth"
    condition:
        all of them
}
rule getUTCSeconds_ : js {
    strings:
        $ = "getUTCSeconds"
    condition:
        all of them
}
rule getYear_ : js {
    strings:
        $ = "getYear"
    condition:
        all of them
}
rule includes_ : js {
    strings:
        $ = "includes"
    condition:
        all of them
}
rule indexOf_ : js {
    strings:
        $ = "indexOf"
    condition:
        all of them
}
rule isArray_ : js {
    strings:
        $ = "isArray"
    condition:
        all of them
}
rule isFinite_ : js {
    strings:
        $ = "isFinite"
    condition:
        all of them
}
rule isInteger_ : js {
    strings:
        $ = "isInteger"
    condition:
        all of them
}
rule isNaN_ : js {
    strings:
        $ = "isNaN"
    condition:
        all of them
}
rule isSafeInteger_ : js {
    strings:
        $ = "isSafeInteger"
    condition:
        all of them
}
rule italics_ : js {
    strings:
        $ = "italics"
    condition:
        all of them
}
rule join_ : js {
    strings:
        $ = "join"
    condition:
        all of them
}
rule lastIndexOf_ : js {
    strings:
        $ = "lastIndexOf"
    condition:
        all of them
}
rule link_ : js {
    strings:
        $ = "link"
    condition:
        all of them
}
rule localeCompare_ : js {
    strings:
        $ = "localeCompare"
    condition:
        all of them
}
rule map_ : js {
    strings:
        $ = "map"
    condition:
        all of them
}
rule match_ : js {
    strings:
        $ = "match"
    condition:
        all of them
}
rule now_ : js {
    strings:
        $ = "now"
    condition:
        all of them
}
rule parse_ : js {
    strings:
        $ = "parse"
    condition:
        all of them
}
rule parseFloat_ : js {
    strings:
        $ = "parseFloat"
    condition:
        all of them
}
rule parseInt_ : js {
    strings:
        $ = "parseInt"
    condition:
        all of them
}
rule pop_ : js {
    strings:
        $ = "pop"
    condition:
        all of them
}
rule push_ : js {
    strings:
        $ = "push"
    condition:
        all of them
}
rule random_ : js {
    strings:
        $ = "random"
    condition:
        all of them
}
rule reduce_ : js {
    strings:
        $ = "reduce"
    condition:
        all of them
}
rule reduceRight_ : js {
    strings:
        $ = "reduceRight"
    condition:
        all of them
}
rule repeat_ : js {
    strings:
        $ = "repeat"
    condition:
        all of them
}
rule replace_ : js {
    strings:
        $ = "replace"
    condition:
        all of them
}
rule reverse_ : js {
    strings:
        $ = "reverse"
    condition:
        all of them
}
rule search_ : js {
    strings:
        $ = "search"
    condition:
        all of them
}
rule setDate_ : js {
    strings:
        $ = "setDate"
    condition:
        all of them
}
rule setFullYear_ : js {
    strings:
        $ = "setFullYear"
    condition:
        all of them
}
rule setHours_ : js {
    strings:
        $ = "setHours"
    condition:
        all of them
}
rule setMilliseconds_ : js {
    strings:
        $ = "setMilliseconds"
    condition:
        all of them
}
rule setMinutes_ : js {
    strings:
        $ = "setMinutes"
    condition:
        all of them
}
rule setMonth_ : js {
    strings:
        $ = "setMonth"
    condition:
        all of them
}
rule setSeconds_ : js {
    strings:
        $ = "setSeconds"
    condition:
        all of them
}
rule setTime_ : js {
    strings:
        $ = "setTime"
    condition:
        all of them
}
rule setUTCDate_ : js {
    strings:
        $ = "setUTCDate"
    condition:
        all of them
}
rule setUTCFullYear_ : js {
    strings:
        $ = "setUTCFullYear"
    condition:
        all of them
}
rule setUTCHours_ : js {
    strings:
        $ = "setUTCHours"
    condition:
        all of them
}
rule setUTCMilliseconds_ : js {
    strings:
        $ = "setUTCMilliseconds"
    condition:
        all of them
}
rule setUTCMinutes_ : js {
    strings:
        $ = "setUTCMinutes"
    condition:
        all of them
}
rule setUTCMonth_ : js {
    strings:
        $ = "setUTCMonth"
    condition:
        all of them
}
rule setUTCSeconds_ : js {
    strings:
        $ = "setUTCSeconds"
    condition:
        all of them
}
rule setYear_ : js {
    strings:
        $ = "setYear"
    condition:
        all of them
}
rule shift_ : js {
    strings:
        $ = "shift"
    condition:
        all of them
}
rule slice_ : js {
    strings:
        $ = "slice"
    condition:
        all of them
}
rule small_ : js {
    strings:
        $ = "small"
    condition:
        all of them
}
rule some_ : js {
    strings:
        $ = "some"
    condition:
        all of them
}
rule sort_ : js {
    strings:
        $ = "sort"
    condition:
        all of them
}
rule splice_ : js {
    strings:
        $ = "splice"
    condition:
        all of them
}
rule split_ : js {
    strings:
        $ = "split"
    condition:
        all of them
}
rule startsWith_ : js {
    strings:
        $ = "startsWith"
    condition:
        all of them
}
rule strike_ : js {
    strings:
        $ = "strike"
    condition:
        all of them
}
rule sub_ : js {
    strings:
        $ = "sub"
    condition:
        all of them
}
rule substr_ : js {
    strings:
        $ = "substr"
    condition:
        all of them
}
rule substring_ : js {
    strings:
        $ = "substring"
    condition:
        all of them
}
rule sup_ : js {
    strings:
        $ = "sup"
    condition:
        all of them
}
rule test_ : js {
    strings:
        $ = "test"
    condition:
        all of them
}
rule toDateString_ : js {
    strings:
        $ = "toDateString"
    condition:
        all of them
}
rule toGMTString_ : js {
    strings:
        $ = "toGMTString"
    condition:
        all of them
}
rule toISOString_ : js {
    strings:
        $ = "toISOString"
    condition:
        all of them
}
rule toJSON_ : js {
    strings:
        $ = "toJSON"
    condition:
        all of them
}
rule toLocaleDateString_ : js {
    strings:
        $ = "toLocaleDateString"
    condition:
        all of them
}
rule toLocaleLowerCase_ : js {
    strings:
        $ = "toLocaleLowerCase"
    condition:
        all of them
}
rule toLocaleString_ : js {
    strings:
        $ = "toLocaleString"
    condition:
        all of them
}
rule toLocaleTimeString_ : js {
    strings:
        $ = "toLocaleTimeString"
    condition:
        all of them
}
rule toLocaleUpperCase_ : js {
    strings:
        $ = "toLocaleUpperCase"
    condition:
        all of them
}
rule toLowerCase_ : js {
    strings:
        $ = "toLowerCase"
    condition:
        all of them
}
rule toString_ : js {
    strings:
        $ = "toString"
    condition:
        all of them
}
rule toTimeString_ : js {
    strings:
        $ = "toTimeString"
    condition:
        all of them
}
rule toUTCString_ : js {
    strings:
        $ = "toUTCString"
    condition:
        all of them
}
rule toUpperCase_ : js {
    strings:
        $ = "toUpperCase"
    condition:
        all of them
}
rule trim_ : js {
    strings:
        $ = "trim"
    condition:
        all of them
}
rule unescape_ : js {
    strings:
        $ = "unescape"
    condition:
        all of them
}
rule unshift_ : js {
    strings:
        $ = "unshift"
    condition:
        all of them
}
rule valueOf_ : js {
    strings:
        $ = "valueOf"
    condition:
        all of them
}
